[package]
name = "scanner-ingest"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "cyber-posture scanner HTTP ingest client (form upload planned)"

[dependencies]
anyhow = "1.0.102"
scanner-contract = { version = "0.1.0", path = "../scanner-contract" }

[lints]
workspace = true
