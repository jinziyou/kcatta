[package]
name = "scanner-snapshot-contract"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "cyber-posture remote snapshot backend contract (RemoteExec, SnapshotBackend, RemoteSnapshot)"

[dependencies]
anyhow = "1.0.102"

[lints]
workspace = true
