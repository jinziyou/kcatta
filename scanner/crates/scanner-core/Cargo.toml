[package]
name = "scanner-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "cyber-posture scanner facade (re-exports runtime + default plan)"

[dependencies]
anyhow = "1.0.102"
scanner-asset = { version = "0.1.0", path = "../scanner-asset" }
scanner-contract = { version = "0.1.0", path = "../scanner-contract" }
scanner-runtime = { version = "0.1.0", path = "../scanner-runtime" }

[lints]
workspace = true

[dev-dependencies]
jsonschema = "0.46.5"
serde_json = "1.0.150"
tempfile = "3.25.0"
