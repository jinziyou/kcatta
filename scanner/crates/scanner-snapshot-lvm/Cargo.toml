[package]
name = "scanner-snapshot-lvm"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "cyber-posture LVM snapshot backend (lvcreate -s) for scanner-snapshot-contract"

[dependencies]
anyhow = "1.0.102"
base64 = "0.22.1"
scanner-snapshot-contract = { version = "0.1.0", path = "../scanner-snapshot-contract" }

[lints]
workspace = true
