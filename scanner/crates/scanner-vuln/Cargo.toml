[package]
name = "scanner-vuln"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "cyber-posture vulnerability scanning (trivy bridge planned)"

[dependencies]
anyhow = "1.0.102"
scanner-runtime = { version = "0.1.0", path = "../scanner-runtime" }

[lints]
workspace = true
