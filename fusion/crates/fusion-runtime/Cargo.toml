[package]
name = "fusion-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "posture host scan orchestration (Collector trait, run_scan)"
readme = "README.md"

[dependencies]
anyhow = { workspace = true }
chrono = { workspace = true, features = ["serde", "clock", "std"] }
fusion-contract = { version = "0.1.0", path = "../fusion-contract" }
uuid = { workspace = true }

[lints]
workspace = true

[dev-dependencies]
jsonschema = { workspace = true }
fusion-asset = { version = "0.1.0", path = "../fusion-asset" }
serde_json = { workspace = true }
tempfile = { workspace = true }
