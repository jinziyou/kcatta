[workspace]
resolver = "2"
members = [
    # Shared base (each dir IS a crate, no nesting):
    "crates/contract",   # data contract: AssetReport + FlowBatch + GuardEventBatch
    "crates/ingest",     # HTTP upload client → fusion (depends only on contract)
    "crates/cli-common", # shared CLI plumbing: JSON output sink + blocking HTTP client
    # Three capabilities — one directory each, lib + bin in the same crate:
    "crates/host",       # posture-host:  host static file detection (+ built-in malware scan)
    "crates/flow",       # posture-flow:  traffic detection (capture + IOC + intel-sync)
    "crates/guard",      # posture-guard: real-time protection daemon (detect + respond + report)
    # Umbrella: a single `agent` binary aggregating the three capability CLIs.
    "crates/agent",      # posture-agent: the unified `agent host|flow|guard` command
]

# posture agent workspace — host + network telemetry collection.
# See README.md and docs/ARCHITECTURE.md.
[workspace.package]
edition = "2021"
license = "Apache-2.0"
publish = false
repository = "https://github.com/jinziyou/posture"

# Shared third-party dependencies. Per-crate manifests pin the version here and
# only add the features they need (features are additive across the workspace).
[workspace.dependencies]
anyhow = "1.0.102"
chrono = { version = "0.4.44", default-features = false }
clap = { version = "4.6.1", features = ["derive"] }
jsonschema = "0.46.5"
reqwest = { version = "0.12.28", default-features = false, features = ["blocking", "rustls-tls"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
tempfile = "3.25.0"
uuid = { version = "1.23.1", features = ["v4"] }
walkdir = "2.5.0"

[workspace.lints.rust]
unsafe_code = "deny"
missing_docs = "warn"
