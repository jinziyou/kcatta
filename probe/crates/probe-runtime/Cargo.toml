[package]
name = "probe-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
description = "cyber-posture scanner orchestration (Collector trait, run_scan)"
readme = "README.md"

[dependencies]
anyhow = { workspace = true }
chrono = { workspace = true, features = ["serde", "clock", "std"] }
probe-contract = { version = "0.1.0", path = "../probe-contract" }
uuid = { workspace = true }

[lints]
workspace = true

[dev-dependencies]
jsonschema = { workspace = true }
probe-asset = { version = "0.1.0", path = "../probe-asset" }
serde_json = { workspace = true }
tempfile = { workspace = true }
